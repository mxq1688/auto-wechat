<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- Android 11+ 包可见性声明 -->
    <queries>
        <package android:name="com.tencent.mm" />
        <!-- 语音识别服务 -->
        <package android:name="com.iflytek.speechsuite" />
        <package android:name="com.heytap.speechassist" />
        <package android:name="com.google.android.googlequicksearchbox" />
        <!-- 语音识别 Intent -->
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.WeChatAssistant"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">
        
        <!-- 主界面 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.WeChatAssistant.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- 视频通话界面 -->
        <activity
            android:name=".ui.VideoCallActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.WeChatAssistant.NoActionBar" />
        
        <!-- 语音设置界面 -->
        <activity
            android:name=".ui.VoiceSettingsActivity"
            android:theme="@style/Theme.WeChatAssistant.NoActionBar" />
        
        <!-- 设置界面 -->
        <activity
            android:name=".ui.SettingsActivity"
            android:theme="@style/Theme.WeChatAssistant.NoActionBar" />
        
        <!-- 增强版无障碍服务 -->
        <service
            android:name=".service.EnhancedWeChatAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
        
        <!-- 悬浮球服务 -->
        <service
            android:name=".service.FloatingBallService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
        
        <!-- 通知监听服务 - 用于自动接听微信视频通话 -->
        <service
            android:name=".service.CallNotificationListenerService"
            android:exported="true"
            android:label="微信视频通话自动接听"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
        
        <!-- 语音监听前台服务 -->
        <service
            android:name=".service.VoiceListeningForegroundService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- 坐标采集服务 -->
        <service
            android:name=".service.CoordinatePickerService"
            android:exported="false" />
        
        <!-- 拨打电话遮罩服务 -->
        <service
            android:name=".service.CallOverlayService"
            android:exported="false" />
        
        <!-- FileProvider 用于拍照 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
            
    </application>
</manifest>
